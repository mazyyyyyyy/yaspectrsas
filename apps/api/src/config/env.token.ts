/**
 * Токен внедрения для провалидированного окружения.
 *
 * Отдельным файлом, чтобы env.ts оставался чистым модулем без зависимости
 * от Nest — его вызывает и main.ts до старта приложения, и тесты.
 */
export const ENV = Symbol('APP_ENV');
